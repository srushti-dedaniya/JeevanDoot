# Frontend API configuration
# Set to false to talk to the real backend (Express + MongoDB).
# Keep true to run the app with in-memory mock data (no server needed).
VITE_ENABLE_MOCK_API=false

# Base URL of the backend API. The backend mounts routes under /api/v1.
VITE_API_BASE_URL=http://localhost:5000/api/v1
